using System.Collections;
using System.Collections.Generic;
using UnityEngine;
    
public class NewBehaviourScript : MonoBehaviour
{

    /*
    I want to have such capabilities
    1. Enable/Disable specific UI element
    2. Enable/Disable group of UI elements
    3. Show UI element on the top of all UI elements, blocking interaction with them (Popup)
    4. Show UI element with disabling groups of elements
    5. Show Popup over another Popup    
    6. Stack Popups, Hiding Previous Popups
    7. Open popup, closing Previous Popups
    8. Have a distinction between Closing popup and Hiding Popup 
    9. Have control to show Popup with or without animation
    10. Have control to offset for camera
    11. Set UI interactable or not
    */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
